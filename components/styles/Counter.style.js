import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
        justifyContent: 'center'
    },
    counterText: {
        fontSize: 100,
        textAlign: 'center'

    }
});